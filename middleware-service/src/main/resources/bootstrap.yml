server:
  port: 8080

spring:
  application:
    name: middleware-service
  profiles:
    active: dev
  cloud:
    config:
      uri: http://localhost:9080

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8888/eureka/